apiVersion: v1
kind: Service
metadata:
  name: server
  labels:
    app: server
spec:
  type: ClusterIP
  selector:
    app: server
  ports:
    - name: ws
      port: 8080
      targetPort: ws
      protocol: TCP
    - name: http
      port: 8081
      targetPort: http
      protocol: TCP
